<!-- Thanks for the PR! Keep it focused; small PRs merge faster. -->

**What & why**
A sentence or two.

**How I tested**
e.g. `swift test`, launched and verified, `./Scripts/uitest.sh`.

**Checklist**
- [ ] `swift test` is green
- [ ] Matches the surrounding style
- [ ] Shell commands go through `CommandTemplate` (no hand-built command/AppleScript strings)
- [ ] Updated docs/README if behavior changed
